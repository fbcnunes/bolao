services:
  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:3000:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always
    env_file:
      - ./app/.env
